name: example
description: An example application to demonstrate showcase_tutorial package.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  showcase_tutorial:
    path: ../


dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/